services:
  shopping-list:
    build: .
    expose:
      - "8321"
    environment:
      - APP_ENV=production
      - APP_PASSWORD=${APP_PASSWORD:-shopping123}
      - PORT=8321
      - DB_PATH=/data/shopping.db
    volumes:
      - shopping-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:8321/login"]
      interval: 30s
      timeout: 3s
      retries: 3

volumes:
  shopping-data:
